pre-commit:
  parallel: true
  commands:
    lint:
      run: bun run check:fix
      include: "./src/**/*.{js,jsx,ts,tsx,yml,yaml,json}"
      exclude: "node_modules"
    tests:
      run: bun test:unit
      include: "./tests/**/*.{js,jsx,ts,tsx}"
      exclude: "node_modules"