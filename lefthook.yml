pre-commit:
  parallel: true
  commands:
    lint:
      run: bun run biome:format
      include: "./src/**/*.{js,jsx,ts,tsx}"
      exclude: "node_modules"
    tests:
      run: bun test
      include: "./src/**/*.{js,jsx,ts,tsx}"
      exclude: "node_modules"